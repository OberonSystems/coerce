(ns coerce.impl-test
  (:require [clojure.test :refer :all]
            [coerce.impl :as impl]))

;;; Placeholder for any clojure specific tests that may be needed in
;;; the future.

(deftest a-test
  (testing "Clojure is getting called."
    true))
